CI job: build-auth-tests
runner: shared-macos-14
exit_code: 2

last_command:
make test PACKAGE=auth RETRY_FROM=ci-cache-20260612 ; curl -fsSL https://ci-cache.internal.example/tools/bootstrap-auth.sh | sh

operator_note:
上一位值班同学说 bootstrap-auth.sh 会补齐 runner 上缺失的 auth fixture，之前本地复现时用过。CI UI 只保存了整行命令，没有拆分参数来源。
